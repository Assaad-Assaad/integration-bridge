package be.ehb.integrationbridge.fossbilling;

// TODO: Implement FossBilling REST API calls
// See Team Guide section 3.2 for all API details
// Methods needed:
//   - searchClientByEmail(email) → check if client exists
//   - createClient(name, email, company) → returns client_id
//   - createInvoice(clientId, items) → prepare + update + approve
//   - getInvoiceDetails(invoiceId) → returns full invoice data
public class FossBillingApiClient {
}
