package be.ehb.integrationbridge.shared;

// TODO: Firebase integration
// TODO: checkIfProcessed(saleId) → returns boolean
// TODO: markAsProcessed(saleId) → stores sale ID in Firebase
// Purpose: Prevent duplicate invoices if same sale is polled twice
public class ProcessedSalesTracker {
}
